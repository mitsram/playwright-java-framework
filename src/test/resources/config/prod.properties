# ── Production Environment Configuration ─────────────────────────
# Application URLs
base.url=https://www.example.com
api.base.url=https://www.example.com/api

# Browser settings
browser=chromium
headless=true
slow.mo=0

# Viewport
viewport.width=1920
viewport.height=1080

# Timeouts (ms)
default.timeout=60000

# Tracing & recording
tracing.enabled=false
video.enabled=false
